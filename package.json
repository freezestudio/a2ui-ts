{
  "name": "a2ui-ts",
  "version": "0.1.0",
  "private": true,
  "description": "A2UI v1.0 TypeScript reference implementation — protocol SDK, framework-agnostic renderer core, Angular renderer, conformance & eval",
  "packageManager": "pnpm@11.18.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "license": "Apache-2.0"
}
